{
  "name": "src",
  "version": "1.0.0",
  "description": "一个功能完整的多语言食谱博客网站，具有响应式设计和SEO优化。",
  "main": "index.js",
  "scripts": {
    "build-css": "npx sass css/style.scss css/style.css",
    "watch-css": "npx sass css/style.scss css/style.css --watch",
    "build-tailwind": "npx tailwindcss -i css/style.css -o css/output.css",
    "watch-tailwind": "npx tailwindcss -i css/style.css -o css/output.css --watch",
    "build": "npm run build-css && npx tailwindcss -i css/style.css -o css/output.css --minify",
    "dev": "npm run build-css && npx tailwindcss -i css/style.css -o css/output.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.69.5",
    "tailwindcss": "^3.3.5"
  }
}